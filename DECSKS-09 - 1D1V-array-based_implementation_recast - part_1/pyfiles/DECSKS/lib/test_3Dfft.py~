import numpy as np

# tier 1

y = np.sin(x)
y2 = np.sin(2*x)
y3 = np.sin(3*x)

# tier 2
yy = np.cos(x)
yy2 = np.cos(2*x)
yy3 = np.cos(3*x)


Y = np.zeros([2, x.shape[0], 3])

for n in range(1,3+1):
    Y[0,:,n-1] = np.sin(n*x)
    Y[1,:,n-1] = np.cos(n*x)
